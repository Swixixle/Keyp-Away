# Example manifest produced by the scanner (`keysmith doctor` conceptual output)
project: detected-project-name
env_file_vars:
  FEC_API_KEY: present # presence only — never literal values
credentials:
  fec:
    env: FEC_API_KEY
    detected_in:
      - adapters/fec.py:45
    provider: fec
