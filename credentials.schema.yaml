# YAML manifest produced by ``keysmith doctor`` / scanner (conceptual schema)
project: detected-project-name
credentials:
  fec:
    env: FEC_API_KEY
    detected_in:
      - adapters/fec.py:45
    provider: fec
    scope: public_read
